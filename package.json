{
  "name": "mimo-chatbot-assignment",
  "version": "1.0.0",
  "private": true,
  "description": "MiMo chat assignment with an Express proxy backend and static frontend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0"
  }
}
